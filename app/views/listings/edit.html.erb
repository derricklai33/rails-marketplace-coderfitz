<div class='header-for-page-div'>
  <h1 class='text-center'>Edit Listing</h1>
</div>

<div class='row'>
  <div class='col-5'>
  <%# Placeholder to push form to the right %>
  </div>
  <div class='col-7'>
    <%= form_with model: @listing, local: true do |f| %>
      <div>  
        <%= f.label :price %><br>
        <%= f.number_field :price %>
      </div>
      <div>  
        <%= f.fields_for :location do |a| %>
        <%= a.label :city %> <br>
        <%= a.text_field :city %> <br>
        <%= a.label :state %> <br>
        <%= a.text_field :state %> <br>
        <%= a.label :country %> <br>
        <%= a.text_field :country %> <br>
        <% end %>
      </div><br>
      <div>
        <%= f.label :images %><br>
        <%= f.file_field :images, accept: 'image/jpg, image/jpeg, image/png', multiple: true %>
      </div><br>
      <%= f.submit "Edit Listing", class: 'btn btn-dark' %>
    <% end %>
  </div>
</div>
  
<%# Javascript for displaying different form items when different buttons are clicked %>
<%= javascript_pack_tag "listing_form" %>